# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: test on push on main / develop
"on":
  push:
    branches:
      - main
      - develop
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: npm install && jest
